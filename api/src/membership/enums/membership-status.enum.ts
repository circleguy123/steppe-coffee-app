export enum MembershipStatus {
  Idle = 'idle',
  Active = 'active',
  Canceled = 'canceled',
  Failed = 'failed',
  Expired = 'expired',
  Prolonged = 'prolonged',
  RetryLimitReached = 'retry_limit_reached',
}
